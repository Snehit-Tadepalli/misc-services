const MILLI_SECOND = 1;
const SECOND = MILLI_SECOND * 1000;
const MINUTE = SECOND * 60;
const HOUR = MINUTE * 60;
const DAY = HOUR * 24;
const WEEK = DAY * 7;

module.exports = { MILLI_SECOND, SECOND, MINUTE, HOUR, DAY, WEEK };
